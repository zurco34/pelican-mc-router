name: Backend CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: backend-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lifecycle:
    name: Disposable control-plane lifecycle
    runs-on: ubuntu-latest
    timeout-minutes: 10

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: backend/go.mod
          cache: true
          cache-dependency-path: backend/go.sum

      - name: Exercise bootstrap lifecycle
        run: >-
          go test -race -count=1
          ./internal/app
          -run '^TestRunLifecycleBootstrapSetupRestartAndShutdown$'

  verify:
    name: Test, vet, and build
    runs-on: ubuntu-latest
    timeout-minutes: 20

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: backend/go.mod
          cache: true
          cache-dependency-path: backend/go.sum

      - name: Verify formatting
        run: |
          files="$(gofmt -l .)"
          if [ -n "${files}" ]; then
            echo "The following files are not formatted:"
            echo "${files}"
            exit 1
          fi

      - name: Verify modules
        run: go mod verify

      - name: Install Go vulnerability scanner
        run: go install golang.org/x/vuln/cmd/govulncheck@v1.1.4

      - name: Scan dependencies for vulnerabilities
        run: govulncheck ./...

      - name: Vet
        run: go vet ./...

      - name: Test with race detector
        run: go test -race -count=1 ./...

      - name: Build server
        run: >-
          go build
          -trimpath
          -o "${RUNNER_TEMP}/pelican-mc-router"
          ./cmd/server
