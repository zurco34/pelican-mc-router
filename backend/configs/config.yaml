server:
  host: "0.0.0.0"
  port: 8080

pelican:
  url: ""
  api_key: ""
  timeout: 15s

discovery:
  interval: 30s

router:
  backend: "infrared"
  domain: ""

infrared:
  config_path: "/etc/infrared/config.json"
  reload_signal: "SIGHUP"

logging:
  level: "info"
  format: "json"